#include "main.h"
/**
 * char *_strdup(char *str) - creates si
 * @str: string
 * Return: something
 */
char *_strdup(char *str)
{
	char *s;

	if (str == NULL)
	{
		return (NULL);
	}
	else 
	{
		return (s = str);
	}
}
